# v0.7 Hugging Face physical MLP evidence

On cached `HuggingFaceTB/SmolLM2-135M`, channel 0 was removed from all 30 gated MLP layers.
The global intermediate width changed from 1,536 to 1,535; every gate/up shape became
`[1535, 576]` and every down shape became `[576, 1535]`.

- Parameter count: 134,515,008 → 134,463,168 (exact delta -51,840)
- Pre-repair mask/physical maximum absolute logit difference: 0.125
- Pre-repair mean absolute logit difference: 0.00001419
- Pre-repair relative L2 logit error: 0.00008869
- `save_pretrained` and local `from_pretrained`: succeeded
- Reload-versus-physical maximum absolute logit difference: 0.0

The isolated gated-model test additionally matches masked and physical outputs at
`rtol=1e-6, atol=1e-7`, verifies the exact parameter formula, serializes state, reconstructs
from the updated width, and reloads successfully.
