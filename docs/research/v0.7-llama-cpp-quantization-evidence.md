# v0.7 pinned llama.cpp quantization evidence

Issue #160 was exercised with the exact CPU-only llama.cpp build used by the
v0.7 load, perplexity, and throughput checks.

## Tool provenance

- expected llama.cpp revision:
  `95b8e33e16bb9a60de780a70930ebf729db6a90a`
- sibling `llama-cli` reported revision: `95b8e33e1`
- `llama-quantize` SHA-256:
  `fc6b702cb88223c5678713e0c9ff20dd68c233fbcf4c3f4032360f322d1180e5`
- recipe: `Q4_K_M`; threads: 6
- source F16 GGUF SHA-256:
  `d5a5af7800b199790a8aeaa633c7b9cb3ce10a5cdc384ac1cbc387344b495315`

The quantizer's `--help` command returned its expected status 1, advertised the
Q4_K_M recipe, and was captured without truncation. No binary is stored in the
repository.

## Real quantization and validation

The transactional command returned status 0 without timeout or log truncation.
The published result was 105,453,632 bytes with SHA-256
`b505aa5616edb1a9d25a097be2350bb980896481003db33efd52a90019c3b7bf`.
Its `general.file_type` is 15 and its 272 tensors comprise:

| Storage type | Tensor count |
| --- | ---: |
| F32 | 61 |
| Q8_0 | 15 |
| Q5_0 | 166 |
| Q6_K | 14 |
| Q4_K | 16 |

The first structural-validation attempt exposed Q5_0 as a mixed-recipe storage
type missing from ModelSurgeon's index. The parser now has pinned, index-only legacy
block layouts, with tests that keep them outside the native codec conformance set.

Finally, pinned `llama-cli` loaded the Q4_K_M artifact and completed the one-token
forward/generation check with status 0, no timeout, and no truncated logs. This
artifact is the real quantized input for v0.7 issue #162.
