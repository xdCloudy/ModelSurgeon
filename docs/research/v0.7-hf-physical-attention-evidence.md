# v0.7 Hugging Face physical attention evidence

On cached `HuggingFaceTB/SmolLM2-135M`, one complete GQA group was removed from every layer:

- Query heads: 9 → 6; KV heads: 3 → 2; fixed head dimension: 64
- Exact whole-model parameter delta: -8,847,360
- Grouped-head mask versus physical logits: 0.0 mean absolute and 0.0 relative L2 error
- `save_pretrained` and local `from_pretrained`: succeeded
- Reload-versus-physical maximum absolute logit difference: 0.0

Focused tests also reject a partial GQA group before any tensor shape changes and verify the
exact Q/K/V/O shapes, group metadata, and parameter delta on a two-layer reference model.
